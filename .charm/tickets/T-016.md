---
id: T-016
title: Character definitions data module (10 named characters)
status: complete
stage: done
depends_on: []
touches:
  - twitch_playground/assets/character_defs.py
  - tests/test_character_defs.py
---
Pure-data character definitions. No deps; runs in parallel with the extraction ticket.

SPEC (authoritative): .charm/kb/conventions/character-def-format.md (dataclass shape + registry contract), .charm/kb/domain/lobcorp-10-characters.md (the 10 chars + variant indices), .charm/kb/domain/lobcorp-sprite-sheet-catalog.md (FULL filenames with '-resources.assets-NNNN.png' suffixes).

DELIVERABLES:
1. assets/character_defs.py — PURE DATA, no pygame/PIL/numpy. CharacterLayer(file,variant) and CharacterDef per the convention note. CHARACTER_DEFS: dict[str,CharacterDef] with all 10 chars from the lobcorp-10-characters table, using FULL filenames from the catalog (map the table's abbreviated prefixes + emotion-named eye/brow/mouth sheets to their full filenames). Populate at least the 'default' face keys for every char; fill battle/panic where the table indicates, else reuse closest sheet. DEFAULT_CHARACTER_ID='Standard Agent'. assign_character(username)->str: stable/deterministic via md5(username)%len into a sorted key list; document determinism.
2. tests/test_character_defs.py: all 10 present + keyed by name; DEFAULT_CHARACTER_ID in dict; every def's eyes/eyebrows/mouth have a 'default' key; assign_character deterministic + returns valid key over sample usernames; module imports with no pygame/PIL.

Match surrounding style. Run `uv run --project . pytest tests/test_character_defs.py`; make it pass. If a variant index is uncertain use the table value verbatim with a brief `# calibrate` comment. Edit only files in your touches set.

<!-- CHARM:LOG -->
## Activity

- 2026-06-02T00:59:56.987Z  `worker-002`  plan: Implement T-016: pure-data character definitions. 1. Create twitch_playground/assets/character_defs.py (no pygame/PIL/numpy): - frozen dataclasses CharacterLayer(file, variant) and CharacterDef per character-def-format convention. - Module-level FULL filename constants (catalog '-resources.assets-NNNN.png' forms), mapping abbreviated table prefixes + emotion-named sheets to full filenames. - Helper builders _eyes/_brows/_mouth to populate full face dicts (default required; closed/dead/panic for eyes; default/battle/panic for brows+mouth), with the table's stated sheet+variant going in the 'default' key and emotion keys reusing the closest emotion sheet. - CHARACTER_DEFS: dict[str, CharacterDef] keyed by name, all 10 chars from lobcorp-10-characters table, variant indices verbatim (# calibrate comment on the row note). - DEFAULT_CHARACTER_ID = 'Standard Agent'. - assign_character(username) -> deterministic via md5(username) % len over sorted key list; documented. 2. Create tests/test_character_defs.py covering: 10 present + keyed by name; DEFAULT_CHARACTER_ID in dict; every def's eyes/eyebrows/mouth have 'default'; assign_character deterministic + valid over sample usernames; module imports with no pygame/PIL loaded. 3. Run `uv run --project . pytest tests/test_character_defs.py`; make pass. Match surrounding style (from __future__ import annotations, module docstrings). Edit only files in touches set.
- 2026-06-02T01:01:35.039Z  `worker-002`  stage=testing: character_defs.py + tests implemented; 7/7 ticket tests pass, full suite 32/32 green.
- 2026-06-02T01:02:43.690Z  `worker-002`  status=complete: Committed in 45feda6. tester-004 spawned for review. 7/7 ticket tests pass; full suite 32/32 green.
- 2026-06-02T01:02:43.700Z  `worker-002`  done: T-016 complete: character_defs.py + tests committed (45feda6), review requested (tester-004).
- 2026-06-02T01:03:23.878Z  `tester-004`  done: T-016 PASS. All 7 ticket tests green (tests/test_character_defs.py), full suite 32/32. Every acceptance criterion met; all 10 variant indices transcribed verbatim from the character table, all sheet filenames are full on-disk names per the catalog, purity verified in a fresh interpreter (no pygame/PIL/numpy).
<!-- /CHARM:LOG -->
