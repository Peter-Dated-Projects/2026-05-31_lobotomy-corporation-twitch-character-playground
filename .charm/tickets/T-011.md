---
id: T-011
title: 'HUD: debug overlay + wire into main loop'
status: complete
stage: done
depends_on: []
touches:
  - twitch_playground/render/hud.py
  - twitch_playground/dev.py
  - twitch_playground/main.py
---
Implement the HUD track in docs/design/sidescroller-contract.md (read it; authoritative interface). Read observable state only; never mutate the world or edit sim files.

Deliver: render/hud.py with draw(screen, world, recent_commands) drawn after the scene — viewer count, per-character mode/clip label, rolling log of last ~6 commands; fonts built once. main.py maintains a deque(maxlen~6) of drained commands, calls hud.draw before flip, and a toggle key (e.g. backquote) to show/hide. dev.py HELP mentions the toggle. Don't change the queue/threading model.

Acceptance: `uv run playground` launches in dev mode; toggle shows/hides overlay; J updates count; F/H/L appear in the log and change per-character labels.

<!-- CHARM:LOG -->
## Activity

- 2026-06-01T20:23:09.044Z  `worker-003`  plan: HUD debug overlay (T-011). Touches: render/hud.py, dev.py, main.py. 1. render/hud.py (new): module-level `draw(screen, world, recent_commands)` drawn AFTER scene.draw. Fonts built once (cached at module level, lazy). Renders: - viewer count (len(world.characters)) top-left - per-character mode/clip label panel: "username MODE/clip[ gN]" for each char (capped ~16, "+N more" overflow) - rolling command log bottom-left: last ~6 commands formatted "!cmd args (author)" Semi-transparent backing panels for readability. Reads observable state only (mode.name, clip, group_id); never mutates world. Export LOG_MAXLEN constant for main to size its deque. 2. main.py: maintain `recent = deque(maxlen=hud.LOG_MAXLEN)`; append each drained ChatCommand before handling it; add `hud_visible` bool toggled by backquote (K_BACKQUOTE); call hud.draw(screen, world, recent) after scene.draw when visible, before flip. 3. dev.py: mention the backquote toggle in HELP. Coding against the contract's Character public API (mode: Mode enum, clip: str, group_id). Robust to the in-flight sim/world rewrites since it only reads those attributes. Won't edit settings.py (out of scope) — constants live in hud.py. Verify with `uv run playground` in dev mode.
- 2026-06-01T20:23:11.139Z  `worker-003`  status=running
- 2026-06-01T20:25:20.929Z  `worker-003`  status=complete
- 2026-06-01T20:25:21.163Z  `worker-003`  done: HUD overlay implemented and wired in. render/hud.py draws viewer count + per-character MODE/clip roster + rolling command log; main.py keeps a deque of drained commands and toggles the HUD with backquote; dev.py HELP updated. Verified headlessly: imports/compile clean, hud.draw renders across populated/empty/overflow paths. Tester-004 spawned for interactive acceptance.
- 2026-06-01T20:26:33.291Z  `tester-004`  done: T-011 validated: all 4 acceptance criteria met. Headless smoke test (SDL dummy) confirmed hud.draw renders viewer count, per-character MODE/clip[ gN] roster, and rolling command log against the live World+PlaceholderProvider; F/H/L change labels (GROUPED/walk, EMOTING/hug) and appear in the log; J increments count. Backquote toggle and deque(maxlen=hud.LOG_MAXLEN) wired in main.py; dev.py HELP documents the toggle. Fonts cached once. Read-only, no sim mutation. Could not drive interactive window (no display), but full render path verified.
<!-- /CHARM:LOG -->
