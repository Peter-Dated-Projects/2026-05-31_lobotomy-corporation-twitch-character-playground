---
id: T-019
title: Calibrate out-of-range face variant indices in character_defs
status: complete
stage: done
depends_on: []
touches:
  - twitch_playground/assets/character_defs.py
  - .charm/kb/domain/lobcorp-10-characters.md
---
4-5 of the 10 characters fall back to placeholder because their variant indices exceed what the extractor actually yields. Correct them against real extraction counts and visually verify.

GROUND TRUTH — actual extract_sprites() counts on the real sheets (do NOT re-derive; these were measured against the live asset drop):
- Mouth_1: 1   (KB catalog expected 8 — connected-component is merging them; only index 0 exists)
- Mouth_Battle_1: 8,  Mouth_Panic_1: 8
- Eye_Default_1: 64,  Eye_Panic_1: 8,  Eye_Close_1: 6,  Eye_Dead_1: 4
- EyeBrow_1: 7,  EyeBrow_Battle_1: 14,  EyeBrow_Panic: 15
- Credit_FrontHair: 18,  Credit_RearHair: 7
- Agent: 11,  Agent2: 6,  all Officer_* : 11 (Yesod 12)
- Weapons: Pistol 5, Rifle 2, Hammer 4, Spear 8, BowGun 3, Defaultweapon 1

The renderer (compose_character) uses clothes limb index = 3 + anim_frame%8, i.e. up to index 10 during the walk clip — so a clothes sheet MUST have >=11 variants. Agent2 has only 6, which breaks Battle-Ready Agent's walk clip.

KNOWN BREAKS to fix in twitch_playground/assets/character_defs.py:
1. Officer Chesed / Hod / Malkut / Yesod reference Mouth_1 v4/v3/v2/v5 but Mouth_1 only has index 0. Repoint each to a valid in-range mouth (Mouth_1 v0 is the safe floor; pick what looks right).
2. Battle-Ready Agent uses Agent2 (6 variants) but the walk clip needs clothes index up to 10. Repoint its clothes to an 11-variant sheet (e.g. Agent or an Officer_* sheet) OR otherwise make every referenced index in-range. Confirm no other character indexes past its sheet's count using the table above.

DO: walk EVERY one of the 10 CharacterDefs and assert each layer's variant < that sheet's count above; fix any that exceed it, keeping the intended look as close as possible.

VISUAL VERIFY (assets are present on this machine): write a THROWAWAY headless script (SDL_VIDEODRIVER=dummy, not committed) that builds a LobCorpProvider, calls get_sprite_set on all 10 named characters, and asserts NONE hit the fallback path (e.g. temporarily make compose raise / check the provider warned flag stays false, or assert the composited base differs from placeholder). Save each character's idle frame 0 to /tmp PNGs and read a few back to eyeball they look like distinct LobCorp faces, not blobs. Report what you saw.

UPDATE .charm/kb/domain/lobcorp-10-characters.md: correct the changed variant indices in the table and replace the stale '# calibrate' caveat note with a short 'calibrated against extraction counts on YYYY-MM-DD' line (use the counts above). Keep it accurate to the code.

VERIFY: full `uv run --project . pytest` stays green (fallback path, no assets needed). Match surrounding style. Touch only character_defs.py and the KB note.

<!-- CHARM:LOG -->
## Activity

- 2026-06-02T01:18:13.020Z  `worker-008`  plan: Calibrate out-of-range variant indices in character_defs.py against ground-truth extraction counts. Walked all 10 CharacterDefs vs the counts. Confirmed exactly the ticket's known breaks (no others out of range): 1. Officer Chesed mouth MOUTH_DEFAULT v4 -> v0 (Mouth_1 has only index 0) 2. Officer Hod mouth MOUTH_DEFAULT v3 -> v0 3. Officer Malkut mouth MOUTH_DEFAULT v2 -> v0 4. Officer Yesod mouth MOUTH_DEFAULT v5 -> v0 5. Battle-Ready Agent clothes AGENT2 (6 variants) -> AGENT (11) since walk needs clothes index up to 10 Other indices verified in-range (Yesod brow v6<7, Geburah eyes v15<64, etc.). Helper-defaulted emotion keys all v0, in range. Steps: - Edit character_defs.py: apply the 5 fixes; strip the now-resolved `# calibrate` comments since indices are calibrated. - Update KB note lobcorp-10-characters.md: correct Chesed/Hod/Malkut/Yesod mouth to v0, Battle-Ready Agent clothes to Agent-1521; replace the stale calibration caveat with a 'calibrated against extraction counts on 2026-06-01' line. - Throwaway headless script (SDL_VIDEODRIVER=dummy, not committed): build LobCorpProvider, get_sprite_set on all 10, assert none hit fallback (warned flag stays False / composite differs from placeholder), dump idle frame 0 PNGs to /tmp, eyeball a few. - Run full `uv run --project . pytest` green.
- 2026-06-02T01:21:51.387Z  `worker-008`  status=running: Implementation + headless visual verify done, committed c067918. Requesting tester.
- 2026-06-02T01:22:00.377Z  `worker-008`  status=complete
- 2026-06-02T01:22:06.412Z  `worker-008`  done: T-019 complete (committed c067918, tester-010 spawned). Calibrated 5 out-of-range indices: Chesed/Hod/Malkut/Yesod mouth -> Mouth_1 v0 (sheet has only index 0); Battle-Ready Agent clothes Agent2(6) -> Agent(11) for the walk clip. Verified headlessly: all 10 composite real art, fallback _warned flag stays False; pytest 33 passed. FLAGGING a separate out-of-scope renderer bug found while eyeballing: composited sprites read as blobs, not faces, because compose_character blits full-res part sprites (Head 193x200, FrontHair 235x215) onto the 32x40 canvas without scaling. Captured in the KB note as a KNOWN RENDER ISSUE; needs a per-layer downscale in lobcorp_renderer.py (next ticket).
- 2026-06-02T01:24:40.292Z  `tester-010`  done: T-019 passes. Independently re-verified: (1) all 26 referenced sheet extraction counts match the ticket's ground-truth table exactly (Mouth_1=1, Eye_Default_1=64, Yesod=12, etc.); (2) walked all 10 CharacterDefs incl. emotion-keyed defaults -> zero out-of-range indices; (3) every clothes sheet carries index 10 (walk needs 3+7=10), Agent2 no longer used; (4) headless LobCorpProvider built all 10 with _warned=False (no fallback, distinct real art per char); (5) full pytest 33 passed; (6) diff touches only character_defs.py + KB note. One partial (!): composites don't yet read as faces -- a separate unscaled-layer renderer bug (32x40 canvas vs full-res parts) that the worker documented honestly in the KB note as out of scope for this data-calibration ticket. No code edited by tester; throwaway verify script run from /tmp, not committed.
<!-- /CHARM:LOG -->
