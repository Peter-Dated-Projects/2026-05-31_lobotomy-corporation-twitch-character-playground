---
id: T-010
title: 'World: platform-aware spawn/grouping + scene + command fixes'
status: complete
stage: done
depends_on: []
touches:
  - twitch_playground/sim/world.py
  - twitch_playground/render/scene.py
---
Implement the World track in docs/design/sidescroller-contract.md (read it; authoritative interface). Code to the Character API in the contract; don't edit or wait on Sim-engine files.

Deliver per the contract: World builds default_level(); spawn on ground at GROUND_TOP; update passes platforms in; follow-grouping snaps followers beside the leader on the LEADER's platform (anchor stays put; guard leader.platform None); scene draws platforms + midbottom-anchored characters y-sorted. Fix !follow/!hug/!leave to be observably correct (reproduce headlessly first; hug must revert, not freeze; leave dissolves singletons).

Acceptance: headless World test (spawn, follow/hug/leave, step) shows correct snapping, hug revert, singleton dissolve; scene.draw runs against a real surface.

<!-- CHARM:LOG -->
## Activity

- 2026-06-01T20:24:35.309Z  `worker-002`  plan: T-010 World + scene, coded to the Character API in docs/design/sidescroller-contract.md (NOT the stale top-down character.py; that's T-009's rewrite, in flight). Scope: twitch_playground/sim/world.py, twitch_playground/render/scene.py only. world.py: 1. __init__: self.platforms = default_level() (import sim.platforms). 2. spawn(): place feet on ground at (random x within WALL_MARGIN, settings.GROUND_TOP); drop the random-y top-down placement. 3. update(dt): pass self.platforms into char.update(dt, positions, self.platforms). 4. _arrange_group(): platform-aware. Anchor (members[0]) stays put at its pos. Followers fan out symmetrically beside the leader, with slot.y = leader.platform.top and slot.x clamped to [platform.left, platform.right]. Guard leader.platform is None (airborne) -> fall back to anchor.pos.y for slot.y and screen-margin clamp for x. 5. Verify !follow/!hug/!leave observably correct (hug reverts via EMOTING->prev mode; leave dissolves 1-member clusters). Reproduce headlessly first; tighten command handlers only if a real bug shows. scene.py: side-view. Fill BG; draw platforms (ground index 0 as a floor band down to screen bottom, floating platforms as slabs with a top edge); draw characters midbottom-anchored at feet pos, y-sorted by pos.y; nameplate above each. Local color/thickness consts (don't touch settings.py - Sim-engine owns it). Verification: throwaway headless script in /tmp (committed tests are T-012's scope). Inject GROUND_TOP etc. + set char.platform manually to test World grouping/spawn/scene.draw independent of T-009 landing. scene.draw runs against a real off-screen Surface. Coordination note: world.py and platforms both need settings.GROUND_TOP and a 4-arg character.update, which land with T-009. My committed code is correct against the contract; full live integration depends on T-009 merging.
- 2026-06-01T20:28:29.682Z  `worker-002`  status=running: World + scene implemented to the contract and committed (9f71212). Verified headlessly against landed Sim-engine code: spawn-on-ground, follow-snapping onto leader's platform, hug revert, singleton dissolve, scene.draw on a real Surface. Requesting review.
- 2026-06-01T20:28:36.749Z  `worker-002`  status=complete
- 2026-06-01T20:28:39.774Z  `worker-002`  done: T-010 complete. world.py + scene.py coded to the sidescroller Character API; committed 9f71212. tester-006 spawned for review. KB gotcha added on the World/Character group-slot responsibility split.
- 2026-06-01T20:30:47.563Z  `tester-006`  done: T-010 PASSES all acceptance criteria. Validated commit 9f71212 (world.py + scene.py) against docs/design/sidescroller-contract.md via a headless harness on the project venv (pygame-ce 2.5.7, dummy SDL). Diff is clean and scoped to the two World-owned files (+ a coordination note). All criteria met: platforms built from default_level (ground index 0 full-width); spawn places feet on ground at GROUND_TOP with random in-bounds x; spawned char binds to its surface after a tick; follow forms a group with follower slot.y == leader.platform.top and slot.x clamped to the leader platform span, follower walks to and arrives at the slot; anchor stays put exactly (Vector2(leader.pos) by construction); hug enters EMOTING and reverts to the prior mode (GROUPED) after HUG_DURATION -- does not freeze; leave returns the leaver to WANDER and dissolves the resulting 1-member cluster, removing the empty group; scene.draw renders onto a real off-screen Surface and paints the ground band. No code edits made.
<!-- /CHARM:LOG -->
