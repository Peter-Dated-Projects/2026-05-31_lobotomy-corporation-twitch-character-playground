---
id: T-009
title: 'Sim-engine: sidescroller physics, jump, landing, clips'
status: complete
stage: done
depends_on: []
touches:
  - twitch_playground/settings.py
  - twitch_playground/sim/character.py
  - twitch_playground/sim/platforms.py
  - twitch_playground/sim/steering.py
  - twitch_playground/assets/provider.py
---
Implement the Sim-engine track in docs/design/sidescroller-contract.md (read it; it's the authoritative interface).

Repo is currently BROKEN: platforms.py references settings.GROUND_TOP/GRAVITY/JUMP_SPEED which don't exist. First job: make the sim import and run again.

Deliver per the contract: settings additions (tune tiers reachable in one jump), Character with update(dt, neighbors, platforms) + gravity/jump/landing + platform attr + 'jump' clip selection, horizontal-only steering, and a 'jump' clip in the provider.

Acceptance: `import twitch_playground.sim.world` is clean; a headless loop (Character on ground, default_level() platforms, ~3s of dt) walks, jumps, lands, never falls through ground. Match surrounding style.

<!-- CHARM:LOG -->
## Activity

- 2026-06-01T20:24:19.052Z  `worker-001`  plan: Sim-engine (T-009). Code against docs/design/sidescroller-contract.md. 1. settings.py: ADD sidescroller physics consts (keep existing ones to avoid breaking world.py/main.py): - GROUND_TOP = SCREEN_H-60 (feet-y of ground) - GRAVITY=1200, JUMP_SPEED=580 (apex ~140px > 95px tier gap -> every tier reachable in one jump), WALK_SPEED=60 - JUMP_CHANCE/IDLE_CHANCE (per-sec, *dt), IDLE_PAUSE_MIN/MAX - HSEP_RADIUS/HSEP_Y_BAND/HSEP_PUSH (small horizontal-only separation) This unbreaks platforms.py which references GROUND_TOP/GRAVITY/JUMP_SPEED. 2. steering.py: replace 2D wander with horizontal-only helper: separation_push(pos, neighbors) -> x nudge (only neighbors at similar height within HSEP_RADIUS). Keep clamp_to_bounds removed/unused; character does its own bounds. 3. character.py: rewrite update(dt, neighbor_positions, platforms). Add self.platform attr (Platform|None, None=airborne). - WANDER: grounded chars stroll at facing*WALK_SPEED; random idle-pause and jump (vel.y=-JUMP_SPEED -> airborne) decisions; gravity when airborne; turn-around+clamp at platform edges (grounded) / screen walls; landing_surface() catches a descending char and snaps feet to top -> never falls through ground. - GROUPED: move toward slot at WALK_SPEED, resolve platform from slot, stand. - EMOTING: stationary (ease x to 0, settle via gravity if airborne), revert after HUG_DURATION. - clip: "hug" emoting / "jump" airborne (platform is None) / "walk" moving / "idle". 4. provider.py: add "jump" clip (stretched airborne pose) to SpriteSet built frames. 5. Verify: headless loop — Character on ground + default_level(), ~3s dt steps; assert imports clean, char stays >= a valid surface, walks/jumps/lands, never below GROUND_TOP. Then request_review + complete.
- 2026-06-01T20:28:00.470Z  `worker-001`  stage=testing: Sim-engine implemented and verified headless: walk/jump/land on ground + floating tiers (never falls through, 40 seeds), grouped arrival, hug revert. Clean import of sim.world. Committed 514077d.
- 2026-06-01T20:28:05.532Z  `worker-001`  status=complete
- 2026-06-01T20:28:07.781Z  `worker-001`  done: T-009 Sim-engine complete and committed (514077d). Tester-005 spawned for review.
- 2026-06-01T20:29:23.215Z  `tester-005`  done: T-009 PASS. All acceptance criteria met (verified headless against docs/design/sidescroller-contract.md). Clean import of twitch_playground.sim.world. 40-seed 3s loop: 0 fall-throughs, 40/40 walked, 23/40 hopped, lands on ground and floating tiers; one-way pass-through-while-rising confirmed (jumped up through left tier top=385, landed on it descending). Clips idle/walk/jump selected; jump apex 140px > 95px tier gap (every tier reachable). Grouped char walks to slot and stands (vel=0, mode=GROUPED). Hug reverts after ~1.2s instead of freezing. No code edits made.
<!-- /CHARM:LOG -->
