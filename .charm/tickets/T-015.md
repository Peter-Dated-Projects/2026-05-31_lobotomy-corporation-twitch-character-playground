---
id: T-015
title: Sprite extraction engine + sheet cache + deps/settings foundation
status: complete
stage: done
depends_on: []
touches:
  - pyproject.toml
  - twitch_playground/settings.py
  - twitch_playground/assets/sprite_extraction.py
  - tests/test_sprite_extraction.py
---
Foundational extraction layer for the real LobCorp art pipeline. No deps; unblocks the renderer ticket.

SPEC (authoritative — read, do not re-derive): .charm/kb/architecture/sprite-extraction-algorithm.md, .charm/kb/decisions/0002-connected-component-extraction.md, .charm/kb/domain/lobcorp-sprite-sheet-catalog.md (asset root + sheet layouts).

DELIVERABLES:
1. pyproject.toml: add runtime deps Pillow>=10, numpy>=1.24, scipy>=1.10; keep python >=3.10,<3.11; run `uv lock`.
2. settings.py (ADD, don't remove existing): ASSETS_ROOT from env LOBCORP_ASSETS_ROOT defaulting to os.path.expanduser('~/Downloads/drive-download-20260601T210823Z-3-001'); PARTS_DIR + CLOTHES_DIR (subfolders 'Employee Parts', 'Employee Clothes and Weapons'); LAYER_OFFSETS dict from the layer-stack KB note; SHEET_ROW_TOLERANCE=60. Group under a commented 'LobCorp sprite assets' section.
3. assets/sprite_extraction.py: extract_sprites(path)->list[PIL.Image] (algorithm exactly per KB note); pil_to_surface(img)->pygame.Surface; class SpriteSheetCache that lazily loads+extracts each sheet once, resolves a bare filename against BOTH PARTS_DIR and CLOTHES_DIR, exposes get(filename, variant_index) and a variant count. Importing the module must NOT require a pygame display or that assets exist — fs/pygame access happens only on call.
4. tests/test_sprite_extraction.py (headless, no display, NO real assets): synthesize white-bg RGBA images with colored rects at known positions in tmp_path, assert box count + reading order (single row, and two-row grouping).

Match surrounding style (from __future__ import annotations, type hints, docstring). Run `uv run --project . pytest tests/test_sprite_extraction.py` headless; make it pass. Edit only files in your touches set.

<!-- CHARM:LOG -->
## Activity

- 2026-06-02T00:59:36.053Z  `worker-001`  plan: T-015 plan (4 files, all in touches): 1. pyproject.toml: add runtime deps Pillow>=10, numpy>=1.24, scipy>=1.10 to [project].dependencies (keep python >=3.10,<3.11). Then `uv lock`. 2. settings.py: APPEND a commented 'LobCorp sprite assets' section: ASSETS_ROOT (env LOBCORP_ASSETS_ROOT, default ~/Downloads/drive-download-20260601T210823Z-3-001), PARTS_DIR/CLOTHES_DIR (os.path.join with 'Employee Parts' / 'Employee Clothes and Weapons'), LAYER_OFFSETS dict (from character-layer-stack KB anchor offsets), SHEET_ROW_TOLERANCE=60. Add `import os`. Keep existing content untouched. 3. assets/sprite_extraction.py (new): from __future__ import annotations. extract_sprites(path)->list[Image] exactly per KB algorithm (RGBA, white+transparent mask, scipy ndimage.label, bbox, 5px noise filter, reading-order sort by SHEET_ROW_TOLERANCE). pil_to_surface(img)->pygame.Surface via frombytes+convert_alpha. SpriteSheetCache: lazy per-sheet load+extract, resolve bare filename against PARTS_DIR then CLOTHES_DIR, get(filename, variant_index)->Surface, variant_count(filename)->int. Imports of PIL/numpy/scipy at top; pygame imported at top but NO display/fs access at import time (all in methods/functions). 4. tests/test_sprite_extraction.py (new, headless, no display, no real assets): synthesize white-bg RGBA images with colored rects at known positions in tmp_path; assert extract_sprites box count + reading order for single-row and two-row layouts. No pygame display needed (extract_sprites is pure PIL/numpy). Verify: `uv run --project . pytest tests/test_sprite_extraction.py` headless, make it pass. Then KB notes if any gotcha, request_review, mark complete.
- 2026-06-02T01:01:28.504Z  `worker-001`  stage=testing: All 4 deliverables implemented; test_sprite_extraction.py passes (3/3) headless. Pre-existing failure in tests/test_character_defs.py (T-016, untracked/in-flight, not in my touches) is unrelated: its assert "pygame" not in sys.modules can't hold because conftest.py imports pygame at session scope.
- 2026-06-02T01:02:09.425Z  `worker-001`  status=complete: Committed as 3d5e646. Tester-003 spawned.
- 2026-06-02T01:02:12.245Z  `worker-001`  done: T-015 complete (commit 3d5e646). 4 deliverables: pyproject deps+uv.lock, settings LobCorp section, assets/sprite_extraction.py, headless tests (3/3 pass). Tester-003 requested.
- 2026-06-02T01:03:13.393Z  `tester-003`  done: T-015 validated: all 12 acceptance criteria met. test_sprite_extraction.py passes 3/3 headless. Deps (Pillow/numpy/scipy) present and locked; settings additive with ASSETS_ROOT/PARTS_DIR/CLOTHES_DIR/LAYER_OFFSETS (matches layer-stack KB exactly)/SHEET_ROW_TOLERANCE=60; extract_sprites/pil_to_surface/SpriteSheetCache match KB algorithm; import verified side-effect-free (no display, no assets). Edits confined to touches set (+uv.lock artifact).
<!-- /CHARM:LOG -->
