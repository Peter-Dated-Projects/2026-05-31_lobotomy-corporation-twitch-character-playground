# Conventions

Patterns and idioms *this specific repo* follows -- how code is structured, named, tested.

| Note | Summary | Status |
|---|---|---|
| [character-def-format](character-def-format.md) | Characters are defined as frozen CharacterDef dataclasses with CharacterLayer(file, variant) fields; emotion-keyed face layers use dicts; all 10 named characters live in character_defs.py. | current |
